.row-fluid
  .span6
    = form_for @report do |f|
      -if @report.errors.any?
        #error_explanation
          %h2= "#{pluralize(@report.errors.count, "error")} prohibited this report from being saved:"
          %ul
            - @report.errors.full_messages.each do |msg|
              %li= msg
    
      .field
        = f.label :date
        = f.date_select :date, {}, :class => 'span4'
      .field
        = f.label :body
        = f.text_area :body, :rows => 30, :cols => 30, :class => 'span12'
      .actions.form-actions
        = f.submit 'Save', :class => 'btn btn-primary'
        = link_to 'Cancel', :reports, :class => 'btn'
  .span6
    %section
      .page-header
        %h1 Preview
      #preview
        %p ぷれーすほるだー：日報を入力するとここにプレビューが表示されるよ
    %hr
    %section
      .page-header
        %h1 記入例
      %ul
        %li 今週やること
        %li 今日やったこと
        %li 明日やること
        %li 困ったこと・悩んでいること
        %li 自由に

      %p
        markdown記法が使えます。
        %br
        %a{:href => 'http://github.github.com/github-flavored-markdown/'}
          GitHub Flavored Markdown - Introduction
      
